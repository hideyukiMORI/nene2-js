# createNene2Client API

`createNene2Client(config)` → `Nene2Client`.

System: `frameworkSmoke`, `health({ allowDegraded?, strictService? })`, `machineHealth`, `ping`, `smoke`.

Beispiele: Notes/Tags-CRUD, `getProtected`. Fehler → `Nene2ClientError`.
