## Summary

-

## Related Issue

Closes #

## Verification

- [ ] `npm run check` (or list narrower commands run)
- [ ] If `contracts/*` changed: `npm run contracts:sync`, `npm run codegen`, and `npm run codegen:guards` (commit `src/generated/openapi.ts` and `src/generated/guards.ts`)

## Risks / follow-up

-
