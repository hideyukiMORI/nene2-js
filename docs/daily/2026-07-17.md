# 2026-07-17

統合リナ指揮下で **W2b（invoice の transport 移行）のブロッカー = `recoverAuth` seam** を「調査 → 設計 → 実装 → publish → consumer レビュー」まで一気通貫で完了した。`_work/issues.md #38` がサーバ側 cookie-`Path` バグと実査で確定し、非破壊 opt-in seam ＋ rotation reuse 防御の single-flight ＋ per-mode 昇格ゲートで設計、`@hideyukimori/nene2-client` **1.2.0** を publish、invoice の適用 PR を seam owner 視点でレビュー承認して **W2b core を閉じた**。

---

## `recoverAuth` seam 実装（Issue #107 / PR #108・ADR 0008）

- **触った物**: `src/transport/transport.ts` に `recoverAuth` 設定 ＋ `Nene2Transport.recover()` ＋ `send()` 内の single-flight / 1 replay。`tests/transport/recover-auth.test.ts` 新設。
- **裏取り**: 現 transport の 401 は fail-closed のみ（refresh/retry 無し）と実コードで確認〔実測〕。`#38` = サーバ `RefreshHandler` が rotation 時 `ni_refresh` を slug 抜き `Path` で再発行 → path モードで silent refresh が初回1回きり → family-burn、を `_work/issues.md #38` ＋ `handoff-invoice-demo-2026-07-07.md` で実査確定〔実測〕。テスト 10 本 / transport 全 53 / full 596 緑〔実測〕。
- **★教訓**: reuse 防御（`nene-invoice/src/Auth/RefreshSessionUseCase.php` の family burn〔実測: 該当行確認〕）ゆえ、single-flight は最適化でなく**正当性要件**。app-start probe も `transport.recover()` で 401-retry と同一 in-flight を共有。当初の「probe は app 側」案は invoice 指摘で撤回。

---

## ドキュメント（Issue #109 / PR #110・#111・#114）

- **触った物**: 移行ガイド `docs/howto/migrate-product-client.md` を新設（`transport.ts` の宙吊り参照を解消）。invoice レビューで in-memory posture・1.2.0 前提・per-mode 昇格ゲートを反映。
- **裏取り**: invoice の被影響実測（`apiClient` 40 参照・`shared/api` 直 import 46・auth 公開面の消費者数）を反映〔伝聞: invoice リナ〕。
- **★教訓**: 昇格ゲートは blanket でなく **per-mode**（single/host = `#38` 非該当 = 今すぐ有効化可 / path = `#38` 根治まで withhold）。ADR 0008 §3/Q5 は既に single-safe を明記済みで、adopter ガイドをそれに揃えた。

---

## リリース 1.2.0（Issue #112 / PR #113）

- **触った物**: `package.json` 1.1.0 → 1.2.0 ＋ `CHANGELOG.md [1.2.0]`。施主が `publish.yml`（`workflow_dispatch`・OIDC Trusted Publishing）をトリガして npm publish。
- **裏取り**: npm `latest` = 1.2.0〔実測: `npm view`〕・git tag `v1.2.0` ＋ GitHub Release〔実測〕・published tarball の `dist/transport/transport.d.ts` に `recoverAuth?`（L83）/ `recover()`（L154）/ `isNene2ClientError` / `TokenStore` 実在〔実測: tarball 展開 grep〕。
- **★教訓**: 「merged ≠ published」を published tarball の実測で潰す。`#38` の呼称も GitHub `nene-invoice#38`（組織 CRUD・無関係）≠ `_work/issues.md #38` と自己訂正。

---

## invoice #685 レビュー（consumer 適用）

- **触った物**: `nene-invoice` の `apiClient → transport seam` 移行 PR #685 を seam owner 視点でレビュー → **APPROVE**（gh の self-approve 不可のため COMMENTED レビューに「APPROVE ✅」明記）。
- **裏取り**: in-memory adapter 維持・`recoverAuth` bare fetch・422 `alsoOkStatuses`・`refreshSession → recover()`・per-mode ゲート `...(isPathTenancy ? {} : { recoverAuth })`・`client.test.ts` 無改変で全パス、を diff で実査〔実測〕。network→`status:0`→transport の error 写像も `wrapFetchError` で裏取り〔実測〕。invoice 申告の 319 tests / 40 消費ファイル無変更〔伝聞〕。統合リナがマージ → W2b 完了〔伝聞: hub〕。

---

## 残タスク・申し送り

- **`#38` 根治（案B: rotation cookie を slug `Path`・サーバ側 = invoice backend 手番）** → 完了で path モードの `recoverAuth` 有効化 ＝ 昇格ゲート解除。別レイヤー・nene2-js の担当外。
- **B トラック横展開**（records / clear 等の transport 移行）はガイドが前提文書として整備済み。
- 本 PR で `docs/worklog → docs/daily` に `git mv`（履歴保持）・`docs/daily/README.md` 索引・`AGENTS.md` 参照を追加（フリート日報規約 `_work/daily-report-convention.md` 適用）。

## 📊 本日の数字

- nene2-js PR: **#108**（seam）/ **#110・#111・#114**（ガイド）/ **#113**（release 1.2.0）〔実測〕
- Issue: **#107・#109・#112・#115**〔実測〕
- テスト: recover-auth **10** / transport **53** / full **596** 緑〔実測〕
- npm: `@hideyukimori/nene2-client` **1.2.0** published〔実測〕
- consumer: `nene-invoice` **#685** レビュー承認〔実測〕
